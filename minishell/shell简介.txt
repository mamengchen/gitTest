在Linux环境下实现shell。
１、技术点
进程间通信中－－－管道
进程控制－－－进程替换，进程创建，进程等待（防止产生僵尸进程）
输入和输出重定向－－－dup2（ps是直接打在屏幕上的所以我们应该让它写到咱们在内核中开辟的管道中，grep是从缓冲区中直接读出来所以我们要让它从内核中管道中读数据）
２、功能
实现单个命令，如：ls
命令可以带参数，如：ls -l
可以带有管道符命令
可以后台运
输出ｃｔｒｌ＋ｃ退出
带有错误提示信息
３、程序分析
do_parse函数：
功能：用于分开字符串，例如：ls -l 我们把 ls 与 -l就是写成两个字符串，为了替换用

has_pipe函数：
功能：用于判断字符串中有没有管道符

主函数：
注意
scanf("%[^\n]%*c", cmd);//^\n:在一个字符串中直到遇见\n才停取出来，%*c:取出来\n丢掉。
isspace用于判断一个字符是否是：\t \n \r 空格
fork用于进程的创建
pipe管道的创建，返回两个文件描述符，用来控制读写端
execvp：程序的替换